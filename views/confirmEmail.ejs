<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <link rel="stylesheet" href="/tailwindcss/tailwind.min.css">
  <title>Confirm Email</title>
</head>

<body class="bg-gray-200 p-20">
  <div class="bg-white w-1/3 mx-auto p-10 shadow rounded-sm">
    <span id="success"
      class="<% if(!success) { %> hidden <% } %> block w-full bg-green-400 text-green-900 px-2 py-1 rounded-sm mb-5">
      <ul>
        <% if(success) { %> <%= success[0].msg %> <% } %>
      </ul>
    </span>
    <span id="errors"
      class="<% if(!errors) { %> hidden <% } %> block w-full bg-red-400 text-red-900 px-2 py-1 rounded-sm mb-5">
      <ul>
        <% if(errors) { %> <%= errors[0].msg %> <% } %>
      </ul>
    </span>
    <h1 class="text-2xl font-thin">We have sent you an email, please check your inbox.</h1>
  </div>

</body>

</html>